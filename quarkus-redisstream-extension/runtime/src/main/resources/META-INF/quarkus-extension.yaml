---
artifact: ${project.groupId}:${project.artifactId}:${project.version}
name: "Messaging - Redis Streams Connector"
description: Connect to Redis Streams API with Reactive Messaging
metadata:
  short-name: "redisstream"
  keywords:
    - "redis"
    - "redisstream"
    - "reactive-redisstream"
  #  guide: ... # To create and publish this guide, see https://github.com/quarkiverse/quarkiverse/wiki#documenting-your-extension
  categories:
    - "messaging"
  status: "experimental"
  config:
    - "mp.messaging."
    - "quarkus.messaging."
    - "quarkus.redis."